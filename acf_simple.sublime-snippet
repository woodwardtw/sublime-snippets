<snippet>
	
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>acf_simple</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!--<scope>source.php</scope>-->


<content><![CDATA[

function acf_fetch_${1:this}(){
  global \$post;
  \$html = '';
  \$${1:this} = get_field('${1:this}');

    if( \$${1:this}) {      
      \$html = \$${1:this};  
     return \$html;    
    }

}



]]></content>
</snippet>

